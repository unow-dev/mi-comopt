# Reused Protocol Reference Snapshots

These files are copied from the supplied discussion snapshot for implementer convenience. They are **reference evidence**, not higher-priority v3 normative authority.

- `tiktokCommentBatch-1.0.0.schema.json`: exact initial submitted update-artifact contract reused by v3 step 00.
- `three-class-workset-protocol.js`: existing Comment DB three-class workset/response protocol implementation reused by v3 classification handoff.

Keyword candidate handoff v1 is reused from repository `unow-dev/mi-comopt` commit `398a904069af3e5e1386e412811f9a8275af1d7a`, entry source `package/src/processing/keyword-candidates/handoff-workflow.js` (Git blob `eda0f2471973816dc58b62553f49777b284a66cc`); its imports/dependencies are resolved at the same repository commit. The discussion snapshot intentionally excluded some old/manual candidate-handoff sources, so the full source is not duplicated here. `V3-REQ-KEY-003` is the normative pin.
