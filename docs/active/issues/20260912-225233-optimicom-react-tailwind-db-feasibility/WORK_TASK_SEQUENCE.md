# Work Task Sequence: `optimicom-react-tailwind` UI/UXと現状DBデータの実現可否

## Purpose

`/home/uya/Workspace/tiktok-filter-keywords/docs/active/temp/optimicom-react-tailwind`をUI/UXの完成形として、画面、表示項目、集計、状態表示、操作に必要なデータと現状DBに存在するデータを照合し、現状DBのデータだけで実現可能なものと、追加データ・仕様変更・DB変更が必要で現時点では実現不可のものを明確に分類できる状態にする。

## Task Sequence

- [ ] 1. 要求整理の範囲で、AIエージェントが、対象UIの画面・表示項目・集計・状態表示・操作を洗い出す。
- [ ] 2. 既存実装の範囲で、AIエージェントが、各UI要素が参照するサンプルデータ、固定値、画面内状態、操作結果を特定する。
- [ ] 3. 現行DBの範囲で、AIエージェントが、スキーマ、保存単位、各テーブルの関係、利用可能な属性、実データの件数と分布を確認する。
- [ ] 4. 仕様整理の範囲で、AIエージェントが、UIの要求データとDBの候補データを項目・粒度・時系列・分類・識別子・更新状態の観点で対応付ける。
- [ ] 5. 分類基準の範囲で、人間が、完全実現・条件付き実現・現時点では実現不可の判定境界と、サンプルデータを本番データとして扱う可否を決定する。
- [ ] 6. 実現可否判定の範囲で、AIエージェントが、画面・表示項目・集計・状態表示・操作ごとに、現状DBのみで実現可能か、前提条件があるか、追加データ・仕様変更・DB変更のどれが必要かを分類する。
- [ ] 7. 不足データ分析の範囲で、AIエージェントが、実現不可または条件付きの項目について、不足するデータ・導出ロジック・保持期間・更新契約・DB変更の必要性を整理する。
- [ ] 8. 検証範囲で、AIエージェントが、DBクエリまたは既存の出力・検証手段を用いて、判定根拠となる実データの存在、欠損、重複、時系列粒度、ラベル対応を確認する。
- [ ] 9. 作業結果の範囲で、AIエージェントが、対応表、実現可否の分類、判定根拠、前提条件、未決事項、追加対応候補を分類レポート文書にまとめる。
- [ ] 10. 成果物確認の範囲で、AIエージェントが、分類レポート文書に対象UIの全画面・表示項目・集計・状態表示・操作が含まれ、分類と根拠が追跡可能であることを確認する。

## Work Notes

- 対象UIは `optimicom-react-tailwind` のホーム、集計・現状・推移、コメント一覧、フィルターキーワード候補、ブロックアカウント候補を範囲とする。
- UI案は `src/data.js` のサンプルデータを使用しており、画面表示にはDBからの取得、集計、永続化を伴わない状態が含まれる。
- UI上の6分類（肯定的・中庸的・批判的・対批判・迷惑・対迷惑）と、DB上のthree-classラベル（`direct_nuisance`・`reactive`・`normal`）は同一概念とみなさず、対応関係と変換可否を確認する。
- 現行の `var/comment-history.sqlite3` は user_version 7 で、rich snapshot 8件、comment-batch 1件、コメント観測 27,299件、three-classラベル 24,622件、keyword candidate publication 1件を保持している。`three_class_workset_excluded_comments` はDB上未適用のため、スキーマ定義との差異を判定根拠に含める。
- `docs/archive` 配下は参照しない。
